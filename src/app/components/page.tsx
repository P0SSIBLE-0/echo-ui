import { ComponentsCatalog } from "@/components/catalog/components-catalog";

export const metadata = {
  title: "Echo UI — All Components",
};

export default function ComponentsPage() {
  return <ComponentsCatalog />;
}
